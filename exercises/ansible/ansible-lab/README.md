# Ansible lab

Small lab used to practice Ansible with a Rocky Linux 9 virtual machine.

- `Vagrantfile` creates the virtual machine.
- `inventory.ini` contains the managed hosts.
- `playbooks` contains the main exercises.
- `bonus-exercises` contains exercises about lists, Jinja templates, and Ansible Vault.

Run the commands from `exercises/ansible/ansible-lab`.
