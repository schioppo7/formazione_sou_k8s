# Main playbooks

- `ping.yml` checks the connection.
- `apache_web_server.yml` installs and configures Apache.
- `variables_and_handlers.yml` uses variables and a handler.
- `conditionals.yml` runs tasks based on the operating system family.
